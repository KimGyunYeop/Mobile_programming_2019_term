<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.udong_mp2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".CircleLIst_NavigationDrawer"
            android:label="@string/title_activity_circle_list__navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".circle.Finance.ToSendRegisterActivity" />
        <activity android:name=".circle.Finance.ReceiptRegisterActivity" />
        <activity android:name=".circle.Finance.CheckPaymentActivity" />
        <activity android:name=".circle.Schedule.CheckAttendanceActivity" />
        <activity android:name=".circle.Schedule.ScheduleRegisterFormActivity" />
        <activity android:name=".circleList.CircleRegisterFormActivity" />
        <activity android:name=".circle.CircleMainActivity" />
        <activity android:name=".login.SignUpActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity
            android:name=".circleList.CircleListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>